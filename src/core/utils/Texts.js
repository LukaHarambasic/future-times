import { fontFamily, fontSize, onBackground } from './Consts'

export default {
  body: {
    fontFamily: fontFamily,
    fontSize: fontSize.body,
    color: onBackground,
    align: 'center',
  },
}
